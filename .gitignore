build/
build_gui/
build_gui_lite/
build_lat/
build_latency/
build_suite/
build_suite_asan/
build_tsan/
build_asan/
# v4.3 — symlinks to latest canonical binaries; build.sh maintains
bin/
vendor/
logging/
paper_runs/
data/
# v4.3 — saved Run bundles. User-trained, edge-sensitive
models/
*.o
*.d
plans
.claude/
.chart-venv/
secrets.cfg

# user-tuned config files — never commit. engine.cfg.example tracks the
# canonical defaults; users copy it to engine.cfg and tune locally.
engine.cfg
controller.cfg
backtest.cfg

# ImGui window state — runtime-only, per-machine
foxml_gui.ini
foxml_suite.ini

# session scratch notes — not for the repo
continue.md

# runtime logs — operator state, balance/position/HMAC events
*.log
baseline_run/

# runtime output: per-symbol metrics + order history (e.g., btcusdt_*.csv)
# emitted by engine main loop alongside *.log; same risk class as logs
*_metrics.csv
*_order_history.csv

# secrets — just-in-case (we already pin secrets.cfg above)
.env
.env.*
*.env

# IDE / tooling per-machine output
.vscode/
.idea/
compile_commands.json
.cache/
.clangd/

# private — going-forward rule for edge-sensitive doc surfaces
# (existing tracked files in public history retain their tracking; only
# new files matching these patterns auto-stay-private. backups land in
# workspace plans/ via the standard /sync-workspace skill.)
# *_AUDIT.md          — optimization audits, latency findings, etc.
# FUTURE_*.md         — roadmap / planned-strategy direction
# *-design-notes.md   — versioned design idea backlogs
# *-suggestions.md    — HFT / optimization research outputs
# *-hft-*.md          — HFT-flavored notes
# STRATEGY_AND_CODING_RULES.md — Gemini-distilled coding invariants
*_AUDIT.md
FUTURE_*.md
*-design-notes.md
*-suggestions.md
*-hft-*.md
STRATEGY_AND_CODING_RULES.md

# local Claude Code memory overlays (private — auto-loaded alongside CLAUDE.md)
CLAUDE.local.md
*.local.md
